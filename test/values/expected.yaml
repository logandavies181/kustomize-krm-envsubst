apiVersion: v1
kind: Service
metadata:
  annotations:
    bar: "true"
    baz: "1"
    foo: foo
    zar: "1.1"
  name: the-service
spec:
  ports:
  - port: 8666
    protocol: TCP
    targetPort: 58008
  type: ClusterIP
